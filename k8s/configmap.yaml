apiVersion: v1
kind: ConfigMap
metadata:
  name: pocker-config
  namespace: default
data:
  NEXT_PUBLIC_SIGNALING_SERVER_URL: "ws://signaling-server-service:8080"
  # For external access, you might use:
  # NEXT_PUBLIC_SIGNALING_SERVER_URL: "wss://your-domain.com/signaling" 